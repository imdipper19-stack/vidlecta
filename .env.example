# ============================================
# VidLecta — Environment Configuration
# ============================================
# Скопируй этот файл в .env и заполни значения
# Copy this file to .env and fill in your values
# 
# БЕЗОПАСНОСТЬ: Никогда не коммить .env в git!
# ============================================

# ==========================================
# 🗄️ DATABASE (PostgreSQL)
# ==========================================
# Логин и пароль для подключения к PostgreSQL
# Придумай надёжный пароль (минимум 16 символов)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=vidlecta
DB_USER=vidlecta_user
DB_PASSWORD=CHANGE_ME_SuperSecurePassword123!

# Полный URL для подключения (собирается автоматически)
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# ==========================================
# ⚡ REDIS (Cache & Celery Broker)
# ==========================================
# Redis используется для кэша и очереди задач Celery
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}/0

# ==========================================
# 🔐 SECURITY
# ==========================================
# Секретный ключ для JWT токенов (минимум 32 символа!)
# Сгенерируй командой: openssl rand -hex 32
SECRET_KEY=CHANGE_ME_generate_with_openssl_rand_hex_32

# Время жизни токенов (в минутах)
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ==========================================
# 📦 MINIO (S3-совместимое хранилище)
# ==========================================
# MinIO хранит загруженные видео и аудио файлы
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=vidlecta_minio
MINIO_SECRET_KEY=CHANGE_ME_MinioSecretKey123!
MINIO_BUCKET=vidlecta-uploads
MINIO_SECURE=false

# ==========================================
# 🎙️ WHISPER (AI Transcription)
# ==========================================
# Модели: tiny (быстро) | base | small (баланс) | medium | large (точно, но медленно)
# Для начала используй 'small' — хороший баланс скорости и качества
WHISPER_MODEL=small

# ==========================================
# 📧 EMAIL (MailerSend - Рекомендуется)
# ==========================================
# MailerSend — 3000 бесплатных писем в месяц
# Регистрация: https://www.mailersend.com/
#
# 1. Зарегистрируйся на mailersend.com
# 2. Добавь и верифицируй домен (или используй trial домен)
# 3. Создай API токен: Settings → API Tokens
# 4. Скопируй токен сюда:
#
MAILERSEND_API_KEY=mlsn.your_api_key_here
MAILERSEND_FROM_EMAIL=noreply@vidlecta.com
MAILERSEND_FROM_NAME=VidLecta

# ==========================================
# 📧 EMAIL (SMTP - Альтернатива)
# ==========================================
# Если не хочешь MailerSend, можно использовать SMTP:
#
# --- Gmail ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password (создай на https://myaccount.google.com/apppasswords)
#
# --- Yandex ---
# SMTP_HOST=smtp.yandex.ru
# SMTP_PORT=587
# SMTP_USER=your_email@yandex.ru
# SMTP_PASSWORD=your_password
#
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_NAME=VidLecta
SMTP_FROM_EMAIL=
SMTP_TLS=true

# ==========================================
# 🌐 URLS
# ==========================================
# URL фронтенда (для CORS и ссылок в письмах)
FRONTEND_URL=http://localhost
API_URL=http://localhost/api

# Для production с доменом:
# FRONTEND_URL=https://vidlecta.com
# API_URL=https://vidlecta.com/api

# ==========================================
# 🚀 ENVIRONMENT
# ==========================================
# development | staging | production
ENVIRONMENT=development

# Режим отладки (true только для разработки!)
DEBUG=true

# ==========================================
# 📊 SUBSCRIPTION TIERS
# ==========================================
# Лимиты минут для каждого тарифа
FREE_MINUTES_LIMIT=60
STUDENT_MINUTES_LIMIT=300
PRO_MINUTES_LIMIT=999999

# ==========================================
# 🔧 CELERY (Background Tasks)
# ==========================================
CELERY_BROKER_URL=${REDIS_URL}
CELERY_RESULT_BACKEND=${REDIS_URL}

# ==========================================
# 📝 NOTES
# ==========================================
# 
# Для генерации безопасных ключей используй:
#   Windows PowerShell: [System.Guid]::NewGuid().ToString() + [System.Guid]::NewGuid().ToString()
#   Linux/Mac: openssl rand -hex 32
#
# После заполнения запусти:
#   docker-compose up -d
#
# Проверь логи:
#   docker-compose logs -f
#
